name: add label to issue
on:
   issues:
     types: [opened]
jobs:
  add_label:
     runs-on: ubuntu-latest
     steps:
       - name: add label to issue
       - uses: andymckay/labeler@1.0.2
         with:
           add-labels: "triage, help-wanted"
           ignore-if-labeled: true
       